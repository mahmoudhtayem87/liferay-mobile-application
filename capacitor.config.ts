import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.liferay.demo.mobile',
  appName: 'Liferay Mobile Application',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
