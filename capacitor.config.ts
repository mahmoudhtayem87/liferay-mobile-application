import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Liferay Mobile Application',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
